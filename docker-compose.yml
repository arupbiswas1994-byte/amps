services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: amps
      POSTGRES_USER: amps
      POSTGRES_PASSWORD: amps_dev_only   # change for any real deployment
    ports:
      - "5432:5432"
    volumes:
      - amps_pgdata:/var/lib/postgresql/data

volumes:
  amps_pgdata:
